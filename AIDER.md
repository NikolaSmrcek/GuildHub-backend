# Aider setup

We use [aider](https://aider.chat/) as the AI coding assistant.

The model currently configured is **deepseek-chat** (DeepSeek V2.5).

To run aider:

```bash
aider --model deepseek-chat
```
